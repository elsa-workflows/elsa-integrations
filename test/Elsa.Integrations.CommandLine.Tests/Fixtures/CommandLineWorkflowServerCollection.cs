namespace Elsa.Integrations.CommandLine.Tests.Fixtures;

[CollectionDefinition(nameof(CommandLineWorkflowServerCollection))]
public class CommandLineWorkflowServerCollection : ICollectionFixture<CommandLineWorkflowServer>
{
    // This class has no code, and is never created. Its purpose is simply
    // to be the place to apply [CollectionDefinition] and all the
    // ICollectionFixture<> interfaces.
}